{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "AI Chat Application",
  "scripts": {
    "build": "npm install --prefix backend && npm install && vite build",
    "start": "cd backend && npm start",
    "dev": "vite"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
